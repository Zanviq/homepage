// Canonical public origin. Single source of truth for metadata, robots and
// the sitemap — changing the domain should mean editing only this line.
export const SITE_URL = "https://portfolio.zanviq.dev";
